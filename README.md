# DP_parallel
DP algorithm parallelism achievement
This program is a improvement compared with DP_serializate project.
In this program, we process the data of floating cars with cuda(GPU) in a parallel way.
Actually, it's based on the project of DP_serializate.
